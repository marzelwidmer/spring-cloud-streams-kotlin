spring:
  application:
    name: spring-cloud-streams-kotlin

  cloud:
    function:
#      definition: uppercase|reverse;uppercase
      definition: uppercase
    azure:
      servicebus:
        connection-string: ${AZ_SERVICE_BUS_CONNECTION_STRING}

    stream:
      bindings:
        uppercase-in-0:
          destination: values-topic
        uppercase-out-0:
          destination: values-topic-out
